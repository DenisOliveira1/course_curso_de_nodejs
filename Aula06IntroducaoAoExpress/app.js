/**
 * framework minimalista: simples
 * framework full stack: mais elaborados
 * 
 * npm: repositorio de pacotes, assim como o pip no python
 * npm já vem instalado com o node
 * ------------------ npm -v
 * 
 * na pasta do projeto:
 * npm install nomeDopacote, nesse caso:
 * 
 * ------------------ npm install express --save
 * 
 * o express não vem sozinho, ele tras outras dependencias as quais ele precisa
 * os modeulos instalados sempre só estão disponiveis na pasta onde o comando install foi usado
 */